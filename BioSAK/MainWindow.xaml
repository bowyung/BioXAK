<Window x:Class="BioSAK.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:BioSAK.Localization"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BioSAK"
        mc:Ignorable="d"
        Title="{loc:L BioXAK_Biology_X_Analysis_Kit}" 
        Height="700" Width="1200" 
        Background="#F5F5F5"
        WindowStartupLocation="CenterScreen"
        Icon="/Fig/ICON.png">

    <Window.Resources>
        <Style x:Key="CategoryHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="Margin" Value="10,15,10,5"/>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E8E8E8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Width" Value="25"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/Fig/ICON.png" Width="32" Height="32" RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="{loc:L BioXAK}" FontSize="22" FontWeight="Bold" Foreground="#333" 
                                   VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>

                        <!-- TCGA DATABASE -->
                        <TextBlock Text="{loc:L TCGA_DATABASE}" Style="{StaticResource CategoryHeaderStyle}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Click="TcgaAnalysis_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🧬" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L TCGA_Analysis}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource NavButtonStyle}" Click="GeneIdConverter_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔄" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L Gene_ID_Converter}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Flow Cytometry -->
                        <TextBlock Text="{loc:L FLOW_CYTOMETRY}" Style="{StaticResource CategoryHeaderStyle}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Click="Flow_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📈" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L Flow_Cytometry_Analyzer}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Graphs -->
                        <TextBlock Text="{loc:L GRAPHS}" Style="{StaticResource CategoryHeaderStyle}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Click="GraphsGenerator_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L Graphs_Generator}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- DNA Tools -->
                        <TextBlock Text="{loc:L DNA_TOOLS}" Style="{StaticResource CategoryHeaderStyle}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Click="NucleotideComplementary_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔄" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L Nucleotide_Complementary}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Style="{StaticResource NavButtonStyle}" Click="PrimerDesign_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔬" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L Primer_Design}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- CRISPR -->
                        <TextBlock Text="{loc:L CRISPR_GENE_EDITING}" Style="{StaticResource CategoryHeaderStyle}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Click="sgRNADesigner_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✂️" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L sgRNA_Designer}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- CLONING (NEW) -->
                        <TextBlock Text="{loc:L CLONING}" Style="{StaticResource CategoryHeaderStyle}"
                                   Visibility="Collapsed"/>

                        <Button Style="{StaticResource NavButtonStyle}" 
                                Click="VectorDesigner_Click"
                                Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🧩" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L Vector_Designer}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <!-- Protein -->
                        <TextBlock Text="{loc:L WESTERN_BLOT}" Style="{StaticResource CategoryHeaderStyle}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Click="WesternBlotOrganizer_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#x1F9EB;" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L WB_Organizer}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <TextBlock Text="{loc:L PROTEIN}" Style="{StaticResource CategoryHeaderStyle}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Click="ProteinConcentrationCalculator_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔬" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L BCA_Conc_Calculator}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Chemical -->
                        <TextBlock Text="{loc:L CHEMICAL}" Style="{StaticResource CategoryHeaderStyle}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Click="MWCalculator_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚖️" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L M_W_Calculator}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}" Click="Concalculator_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🧪" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L Concentration_Calculator}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Restriction Enzyme -->
                        <TextBlock Text="{loc:L RESTRICTION_ENZYME}" Style="{StaticResource CategoryHeaderStyle}"/>

                        <Button Style="{StaticResource NavButtonStyle}" Click="RestrictionEnzymePattern_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L RE_Pattern_Predictor}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}" Click="RestrictionEnzymePredictor_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔍" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L RE_Predictor}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="2" Padding="10" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
                    <StackPanel>
                        <!-- 語言切換：只影響介面文字，不影響任何分析結果或匯出內容 -->
                        <Button Style="{StaticResource NavButtonStyle}" Click="Language_Click"
                                ToolTip="Switch interface language / 切換介面語言">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🌐" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{Binding ToggleLabel, Source={x:Static loc:LocalizationManager.Instance}}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}" Click="About_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="ℹ️" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="{loc:L About}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Frame x:Name="MainFrame" 
               Grid.Column="1"
               NavigationUIVisibility="Hidden"
               Navigated="MainFrame_Navigated"/>
        <Image x:Name="Welcome" Margin="92,-7,-128,10" Source="/Fig/NAME.png" Stretch="Fill" Grid.ColumnSpan="2" IsHitTestVisible="False"/>
    </Grid>
</Window>
