<Window x:Class="BioSAK.LineSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:BioSAK.Localization"
        Title="{loc:L Line_Settings}" 
        Height="320" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{loc:L Line_Settings}" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Line Thickness -->
        <GroupBox Grid.Row="1" Header="{loc:L Line_Thickness_2}" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Slider x:Name="ThicknessSlider" Width="200" Minimum="1" Maximum="10" 
                        Value="2" TickFrequency="0.5" IsSnapToTickEnabled="True"
                        ValueChanged="ThicknessSlider_ValueChanged"/>
                <TextBlock x:Name="ThicknessLabel" Text="2.0" Margin="15,0,0,0" 
                           VerticalAlignment="Center" FontWeight="SemiBold"/>
            </StackPanel>
        </GroupBox>

        <!-- Line Color -->
        <GroupBox Grid.Row="2" Header="{loc:L Line_Color_2}" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Border x:Name="ColorPreview" Width="40" Height="25" Background="Black"
                        BorderBrush="#CCC" BorderThickness="1" CornerRadius="3"/>
                <Button Content="{loc:L Choose_Color_2}" Margin="15,0,0,0" Padding="10,5"
                        Click="ChooseColor_Click"/>
            </StackPanel>
        </GroupBox>

        <!-- Arrow Direction (only visible for arrows) -->
        <GroupBox Grid.Row="3" x:Name="ArrowDirectionGroup" Header="{loc:L Arrow_Direction}" 
                  Padding="10" Margin="0,0,0,10" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="ArrowRightRadio" Content="{loc:L Right}" Margin="0,0,20,0" IsChecked="True"/>
                <RadioButton x:Name="ArrowLeftRadio" Content="{loc:L Left}" Margin="0,0,20,0"/>
                <RadioButton x:Name="ArrowBothRadio" Content="{loc:L Both}"/>
            </StackPanel>
        </GroupBox>

        <!-- Preview -->
        <GroupBox Grid.Row="4" Header="{loc:L Preview}" Padding="10">
            <Canvas x:Name="PreviewCanvas" Background="White" Height="50"/>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="{loc:L Cancel}" Width="80" Padding="5,8" Margin="0,0,10,0"
                    Background="#EEE" BorderThickness="0" Cursor="Hand" Click="Cancel_Click"/>
            <Button Content="{loc:L Apply}" Width="80" Padding="5,8"
                    Background="#4FC3F7" Foreground="White" BorderThickness="0" 
                    Cursor="Hand" Click="Apply_Click"/>
        </StackPanel>
    </Grid>
</Window>
