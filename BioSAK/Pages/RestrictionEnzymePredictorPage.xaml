<Page x:Class="BioSAK.Pages.RestrictionEnzymePredictorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:BioSAK.Localization"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BioSAK.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="{loc:L Restriction_Enzyme_Predictor}"
      Background="WhiteSmoke">

    <Page.Resources>
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <Style x:Key="InputBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        
        <Style x:Key="FilterButtonStyle" TargetType="ToggleButton">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,5,5"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#4CAF50"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title (fix #4: Detcetor → Detector) -->
        <TextBlock Grid.Row="0" 
                   Text="{loc:L RE_Cutting_Site_Detector}" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15"/>

        <!-- Input Section -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Sequence Input -->
            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                <TextBlock Text="{loc:L DNA_Sequence}" Style="{StaticResource HeaderStyle}"/>
                <Border Style="{StaticResource InputBorderStyle}" Margin="0,0,0,10">
                    <TextBox x:Name="SequenceTextBox" 
                             Height="100" 
                             TextWrapping="Wrap" 
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             BorderThickness="0"
                             Padding="5"
                             TextChanged="SequenceTextBox_TextChanged"/>
                </Border>
                
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{loc:L Length}"/>
                    <TextBlock x:Name="SequenceLengthText" Text="{loc:L N0_bp}" FontWeight="SemiBold"/>
                </StackPanel>
            </StackPanel>

            <!-- Options -->
            <StackPanel Grid.Column="1">
                <!-- Topology -->
                <TextBlock Text="{loc:L Topology}" Style="{StaticResource HeaderStyle}"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <RadioButton x:Name="LinearRadio" 
                                 Content="{loc:L Linear}" 
                                 IsChecked="True" 
                                 Margin="0,0,20,0"/>
                    <RadioButton x:Name="CircularRadio" 
                                 Content="{loc:L Circular}"/>
                </StackPanel>

                <!-- Enzyme Filter (fix #2: add Click="Filter_Changed") -->
                <TextBlock Text="{loc:L Enzyme_Filter}" Style="{StaticResource HeaderStyle}"/>
                <WrapPanel Margin="0,0,0,10">
                    <ToggleButton x:Name="Filter4Cutter" 
                                  Content="{loc:L N4_cutter}" 
                                  Style="{StaticResource FilterButtonStyle}"
                                  IsChecked="True"
                                  Click="Filter_Changed"/>
                    <ToggleButton x:Name="Filter6Cutter" 
                                  Content="{loc:L N6_cutter}" 
                                  Style="{StaticResource FilterButtonStyle}"
                                  IsChecked="True"
                                  Click="Filter_Changed"/>
                    <ToggleButton x:Name="Filter8Cutter" 
                                  Content="{loc:L N8_cutter}" 
                                  Style="{StaticResource FilterButtonStyle}"
                                  IsChecked="True"
                                  Click="Filter_Changed"/>
                    <ToggleButton x:Name="FilterOther" 
                                  Content="{loc:L Other}" 
                                  Style="{StaticResource FilterButtonStyle}"
                                  IsChecked="True"
                                  Click="Filter_Changed"/>
                </WrapPanel>

                <!-- Overhang Filter (fix #2: add Click="Filter_Changed") -->
                <TextBlock Text="{loc:L End_Type_Filter}" Style="{StaticResource HeaderStyle}"/>
                <WrapPanel Margin="0,0,0,10">
                    <ToggleButton x:Name="Filter5Prime" 
                                  Content="{loc:L N5_overhang}" 
                                  Style="{StaticResource FilterButtonStyle}"
                                  IsChecked="True"
                                  Click="Filter_Changed"/>
                    <ToggleButton x:Name="Filter3Prime" 
                                  Content="{loc:L N3_overhang}" 
                                  Style="{StaticResource FilterButtonStyle}"
                                  IsChecked="True"
                                  Click="Filter_Changed"/>
                    <ToggleButton x:Name="FilterBlunt" 
                                  Content="{loc:L Blunt}" 
                                  Style="{StaticResource FilterButtonStyle}"
                                  IsChecked="True"
                                  Click="Filter_Changed"/>
                </WrapPanel>

                <!-- Cut Count Filter (fix #2: add Click="Filter_Changed") -->
                <TextBlock Text="{loc:L Show_enzymes_with}" Style="{StaticResource HeaderStyle}"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <CheckBox x:Name="ShowNoCutters" 
                              Content="{loc:L No_cuts_0}" 
                              IsChecked="False"
                              Margin="0,0,15,0"
                              Click="Filter_Changed"/>
                    <CheckBox x:Name="ShowSingleCutters" 
                              Content="{loc:L Single_cut_1}" 
                              IsChecked="True"
                              Margin="0,0,15,0"
                              Click="Filter_Changed"/>
                    <CheckBox x:Name="ShowMultiCutters" 
                              Content="{loc:L Multiple_cuts_2}" 
                              IsChecked="True"
                              Click="Filter_Changed"/>
                </StackPanel>

                <!-- Analyze Button -->
                <Button x:Name="AnalyzeButton" 
                        Content="{loc:L Analyze_All_Enzymes}" 
                        Height="35" 
                        FontSize="14"
                        Background="#2196F3"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="AnalyzeButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Results Section -->
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Summary -->
            <Border Grid.Row="0" 
                    Background="#E3F2FD" 
                    CornerRadius="5" 
                    Padding="10" 
                    Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{loc:L Results}" FontWeight="SemiBold"/>
                    <TextBlock x:Name="ResultsSummaryText" Text="{loc:L Enter_a_sequence_and_click_Analyze}"/>
                </StackPanel>
            </Border>

            <!-- Results Table -->
            <Border Grid.Row="1" Style="{StaticResource InputBorderStyle}">
                <DataGrid x:Name="ResultsDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0"
                          CanUserResizeRows="False"
                          CanUserSortColumns="True"
                          SelectionMode="Single"
                          SelectionChanged="ResultsDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{loc:L Enzyme}" 
                                            Binding="{Binding Enzyme.Name}" 
                                            Width="100"
                                            SortMemberPath="Enzyme.Name"/>
                        <DataGridTextColumn Header="{loc:L Recognition}" 
                                            Binding="{Binding Enzyme.RecognitionSequence}" 
                                            Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Length" 
                                            Binding="{Binding Enzyme.RecognitionSequence.Length}" 
                                            Width="60"/>
                        <DataGridTextColumn Header="{loc:L Cuts}" 
                                            Binding="{Binding CutCount}" 
                                            Width="50"
                                            SortMemberPath="CutCount">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CutCount}" Value="0">
                                            <Setter Property="Foreground" Value="Gray"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding CutCount}" Value="1">
                                            <Setter Property="Foreground" Value="Green"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="{loc:L Cut_Positions}" 
                                            Binding="{Binding PositionsString}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="{loc:L End_Type}" 
                                            Binding="{Binding OverhangDescription}" 
                                            Width="100"/>
                        <DataGridTemplateColumn Header="{loc:L Palindromic}" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Enzyme.IsPalindromic}" 
                                               HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Enzyme.IsPalindromic}" Value="True">
                                                        <Setter Property="Text" Value="✓"/>
                                                        <Setter Property="Foreground" Value="Green"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Enzyme.IsPalindromic}" Value="False">
                                                        <Setter Property="Text" Value="✗"/>
                                                        <Setter Property="Foreground" Value="Gray"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Detail Panel (Popup) -->
        <Popup x:Name="DetailPopup" 
               PlacementTarget="{Binding ElementName=ResultsDataGrid}"
               Placement="Right"
               StaysOpen="False">
            <Border Background="White" 
                    BorderBrush="#CCCCCC" 
                    BorderThickness="1" 
                    CornerRadius="5"
                    Padding="15"
                    MaxWidth="400">
                <StackPanel x:Name="DetailPanel">
                    <TextBlock x:Name="DetailEnzymeName" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="#2196F3"/>
                    <TextBlock x:Name="DetailRecognition" 
                               FontFamily="Consolas" 
                               FontSize="14"
                               Margin="0,5,0,10"/>
                    <TextBlock x:Name="DetailCutInfo" TextWrapping="Wrap"/>
                    <TextBlock x:Name="DetailPositions" 
                               TextWrapping="Wrap" 
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</Page>
