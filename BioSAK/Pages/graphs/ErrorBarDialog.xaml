<Window x:Class="BioSAK.ErrorBarDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:BioSAK.Localization"
        Title="{loc:L Select_Error_Bar_Type}" 
        Height="450" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{loc:L Select_Error_Bar_Type}" FontSize="16" FontWeight="SemiBold" 
                   Foreground="#333" Margin="0,0,0,15"/>

        <!-- Error Bar Type -->
        <GroupBox Grid.Row="1" Header="{loc:L Error_Type}" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <RadioButton x:Name="NoErrorBar" GroupName="ErrorType" Content="None" 
                             Margin="0,3" FontSize="12" IsChecked="True"/>
                <RadioButton x:Name="SDErrorBar" GroupName="ErrorType" Margin="0,3" FontSize="12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{loc:L Standard_Deviation_SD}" FontWeight="SemiBold"/>
                        <TextBlock Text="{loc:L data_spread}" Foreground="#666"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton x:Name="SEMErrorBar" GroupName="ErrorType" Margin="0,3" FontSize="12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{loc:L Standard_Error_of_Mean_SEM}" FontWeight="SemiBold"/>
                        <TextBlock Text="{loc:L precision}" Foreground="#666"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton x:Name="CI95ErrorBar" GroupName="ErrorType" Margin="0,3" FontSize="12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{loc:L N95_Confidence_Interval_95_CI}" FontWeight="SemiBold"/>
                    </StackPanel>
                </RadioButton>
            </StackPanel>
        </GroupBox>

        <!-- Error Bar Direction -->
        <GroupBox Grid.Row="2" Header="{loc:L Error_Bar_Direction}" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <RadioButton x:Name="DirectionBoth" GroupName="Direction" Content="Both" 
                             Margin="0,3" FontSize="12" IsChecked="True"/>
                <RadioButton x:Name="DirectionUp" GroupName="Direction" Content="{loc:L Upper_only}" 
                             Margin="0,3" FontSize="12"/>
                <RadioButton x:Name="DirectionDown" GroupName="Direction" Content="{loc:L Lower_only}" 
                             Margin="0,3" FontSize="12"/>
            </StackPanel>
        </GroupBox>

        <!-- Note -->
        <TextBlock Grid.Row="3" Text="{loc:L Tip_You_can_change_direction_for_individual}" 
                   Foreground="#888" FontSize="11" TextWrapping="Wrap" Margin="0,5"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="{loc:L Cancel}" Width="80" Padding="5,8" Margin="0,0,10,0"
                    Background="#9E9E9E" Foreground="White" BorderThickness="0"
                    Cursor="Hand" Click="Cancel_Click"/>
            <Button Content="{loc:L OK}" Width="80" Padding="5,8"
                    Background="#4CAF50" Foreground="White" BorderThickness="0"
                    Cursor="Hand" Click="OK_Click"/>
        </StackPanel>
    </Grid>
</Window>
