<Window x:Class="BioSAK.ColorPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:BioSAK.Localization"
        Title="{loc:L Choose_Color}" 
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <StackPanel Margin="20">
        <TextBlock Text="{loc:L Select_a_Color}" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Color Grid - continuous blocks, no gaps -->
        <Border BorderBrush="#CCC" BorderThickness="1">
            <UniformGrid x:Name="ColorGrid" Columns="6"/>
        </Border>

        <!-- Custom Color -->
        <GroupBox Header="{loc:L Custom_Color_RGB}" Margin="0,12,0,0" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                    <TextBlock Text="R:" FontSize="11"/>
                    <TextBox x:Name="RedBox" Text="0" TextChanged="RGB_Changed"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,5,0">
                    <TextBlock Text="G:" FontSize="11"/>
                    <TextBox x:Name="GreenBox" Text="0" TextChanged="RGB_Changed"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,5,0">
                    <TextBlock Text="B:" FontSize="11"/>
                    <TextBox x:Name="BlueBox" Text="0" TextChanged="RGB_Changed"/>
                </StackPanel>
                <Border Grid.Column="3" x:Name="PreviewBox" Background="Black" 
                        CornerRadius="5" BorderBrush="#CCC" BorderThickness="1"/>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{loc:L Cancel}" Width="80" Padding="5,8" Margin="0,0,10,0"
                    Background="#EEE" BorderThickness="0" Cursor="Hand"
                    Click="Cancel_Click"/>
            <Button Content="{loc:L OK}" Width="80" Padding="5,8"
                    Background="#4FC3F7" Foreground="White" BorderThickness="0" Cursor="Hand"
                    Click="OK_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
